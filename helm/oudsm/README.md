# OUDSM Helm values

The sanitized values preserve the observed image, ports, service account, worker placement, persistence model, and one-replica initialization. The final PVC size and exact chart key support must be verified.
