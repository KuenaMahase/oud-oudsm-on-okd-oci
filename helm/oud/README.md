# OUD Helm values

`values.example.yaml` is a sanitized recovery of the historical settings. Validate every key against the exact Oracle chart revision before applying it. Use `helm show values`, `helm lint`, `helm template`, and server-side dry-run.
