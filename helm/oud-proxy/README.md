# OUD Proxy configuration

The strongest historical evidence is the `dsconfig` command output, not a complete retained Helm values file. Use the documented `dsconfig` model after deploying two proxy instances from the applicable Oracle chart or workload definition.
